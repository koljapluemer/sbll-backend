Various [models](cms/models) have been drafted.
Let's build a basic CMS.

Always stick to `developer_guidelines.md`!!!!!! and Django best Practices.

Build basic CRUD functionality for language, gloss and situation models.
Use HTML templates (manually crafted) since we're going to have massive customization later; class models or form factories will not help us.

Load daisy+tailwind via CDN. For icons, lucide is installed as a Django plugin `django_lucide_icons`