[project]
name = "sbll-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.8",
    "django-lucide-icons>=0.2.2",
    "openai>=1.58.1",
    "python-dotenv>=1.0.0",
]
